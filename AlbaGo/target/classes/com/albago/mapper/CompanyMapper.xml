<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.albago.mapper.CompanyMapper">

	<select id="getCompanyName" resultType="String">
		select c_name from
		company where c_code=#{c_code}
	</select>

	<select id="getCompanyList" resultType="String">
		select * from history where u_id=#{u_id} and
	<![CDATA[
		h_qdate <> ''
		]]>
	</select>

	<select id="getCompanyListById" resultType="com.albago.domain.CompanyVO">
		select * from company where c_code in (select c_code from history where u_id =#{u_id} and h_qdate is null)
	</select>

</mapper>